## Что сделано
- Ужесточён fallback-поиск topup по сумме: проверка только в безопасном окне конкретного topup, автозачёт только при единственном кандидате.
- В ветке "Мои анализы" клик по матчу теперь сразу открывает анализ (без промежуточного экрана с кнопкой "Показать анализ").
- Добавлена более надёжная загрузка `.env` в `config.py` (fallback без `python-dotenv`).
- `.env.example` очищен до шаблонных значений.

## Проверка
- pytest -q -> 63 passed

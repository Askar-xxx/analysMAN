
Инструкция для Claude Code (CC) — читать и действовать строго по порядку.

1) Прочитай файлы в следующем порядке:
   1. CLAUDE.md (обновлённый блок READ FIRST)
   2. CLAUDE_SYNC_INSTRUCTIONS.md
   3. SPRINT_2_5_UPDATED.md
   4. ROADMAP.md
   5. sprint_2_5_реализовано_добавление_матчей_без_подробной_статистики.txt
   6. Затем просмотреть кодовые файлы: sync_matches.py, database.py, add_test_match.py и каталог tests/

2) После прочтения – выведи (в ответ сюда) следующее:
   A) Краткое резюме (1–3 абзаца):
      - текущее состояние (что реализовано сейчас);
      - целевое состояние (требования из ROADMAP.md).

   B) Таблица несоответствий (формат ASCII/Markdown table):

      Требование (из ROADMAP) | Реализовано? (Yes/No) | Файл(ы) | Комментарий / Что делать
      ----------------------- | --------------------- | ------ | -----------------------
      (пример строки)         | Yes/No                | file.py | краткое действие

   C) Plan of commits — конкретный список атомарных коммитов (по пунктам), например:
      - "cli: add --mode/--limit"
      - "db: add raw_json and source columns + migration script"
      - "sync: save raw_json and source for each event"
      - "limiter: add token bucket rate limiter"
      - "tests: add integration tests for top3 and idempotence"
      и т.д.

   D) Сгенерируй patch/diff (не применяя его):
      - unified diff (git apply compatible) или набор изменённых файлов,
      - подготовленный как PR-ready (включая названия файлов и краткие описания),
      - не вносить никаких изменений в репозиторий — только предоставить diff в ответе.

3) Строгое правило:
   - **НЕ ВНОСИТЬ НИКАКИХ ИЗМЕНЕНИЙ** в код до тех пор, пока я (владелец репо) не подтвержу предложенный план коммитов и diff.

4) Формат ответа:
   - Сначала дать A) краткое резюме;
   - Затем B) таблицу несоответствий;
   - Затем C) план коммитов;
   - В конце прикрепить D) patch/diff (в виде текста unified-diff или отдельных файлов с пометкой "=== FILE: path/to/file ===").

5) Примечания:
   - Читай ROADMAP.md как авторитетный specification.
   - Сохраняй bulk-import функционал (он уже есть) — НЕ УДАЛЯЙ. Bulk должен быть опционален (через флаг).
   - Убедись, что в diff включены миграции для DB и skeleton тестов (pytest).
   - Все коммиты делать в ветку: fix/sync-parametrize-and-trace.

--- Конец инструкции ---

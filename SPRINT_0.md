Прочитай файл ROADMAP.md в корне репозитория и работай строго в рамках Sprint 0: "Setup & Baseline". Не вноси изменений вне задач Sprint 0. Действуй атомарными шагами и присылай патч/дифф для каждого шага (unix diff или полный файл), а также короткий отчёт.

Sprint 0 задачи (выполняй и подтверждай по порядку):
1) РЕВЬЮ: Просмотри следующие файлы и кратко опиши (1-2 предложения) их роль и текущие проблемные места:
   - ai_generator.py
   - utils.py
   - database.py

2) PROMPT: Заменить текущую inline-строку system/user prompt в ai_generator.py так, чтобы промпт загружался из файла ANALYSIS_PROMPT.md при запуске (чтобы дальше можно было менять файл без правки кода). Если файл не найден — падать с понятной ошибкой.

3) POSTPROCESSING: В ai_generator.py / utils.py реализуй/обнови функцию clean_and_truncate(text) с правилами:
   - целевая длина 1200–1800 символов
   - soft cap 2000 — если превышает, автоматически сокращать предложения до целевой длины
   - hard cap 3800 — если всё ещё превышает, обрезать аккуратно по последнему `\n` и добавить '\n...'
   - удалять/фильтровать слова, содержащие корни: "коэффициент", "ставка", "прогноз" (регистронезависимо)
   - разрешать не более 1 эмодзи на раздел и не более 4 эмодзи всего (простая эвристика: разрешённый список можно хранить в коде)
   - логировать (print/log) длину исходного текста и final length и флаг truncated (true/false)

4) SPLIT FOR TELEGRAM: В utils.py обнови/добавь split_for_telegram(text, limit=3800):
   - разрывать сначала по последнему `\n\n` в пределах лимита,
   - если нет — по последнему предложению (по точке/воскл/вопр),
   - если нет — по последнему пробелу,
   - возвращать список частей; каждая часть ≤ 3800.

5) TESTS & CI:
   - Добавь unit тесты в tests/test_ai_generator.py и tests/test_utils.py:
     * тест clean_and_truncate: длинный текст → ≤2000 и не содержит banned words
     * тест split_for_telegram: длинный текст → части ≤3800 и не режется посередине слова
   - Добавь GitHub Actions workflow `.github/workflows/ci.yml` с job:
     * runs-on: ubuntu-latest
     * steps: checkout, setup-python, pip install -r requirements.txt, pip install pytest flake8, run flake8, run pytest
   Присылай оба файла и пример вывода теста (expected commands to run locally).

6) ACCEPTANCE:
   - В конце пришли итоговый чеклист, где каждому шагу соответствует статус (done/ok), и команда проверки (локальная команда)

Ограничения для изменений: не менять логику purchase flow, не подключать новые внешние сервисы, не добавлять новые зависимости кроме pytest/flake8. Если что-то требуется изменить сверх этого — сначала опиши и дождись подтверждения.

Работай пошагово: сначала ревью (шаг 1) и жди моего подтверждения, затем выполняй шаг 2 и т.д.
